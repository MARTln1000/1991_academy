# The database holds real accounts and password hashes. Never commit it.
1991_academy.db
1991_academy.db-wal
1991_academy.db-shm
*.db
*.db-wal
*.db-shm

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Local secrets (SMTP credentials etc.)
.env
.env.*
!.env.example

# Database snapshots from `make backup`: real accounts, same as the .db
backups/

# macOS
.DS_Store
