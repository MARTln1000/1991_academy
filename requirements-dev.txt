# Dev/test only — NOT needed to run the server. Install with:
#   .venv/bin/pip install -r requirements-dev.txt
# Then run the API tests with:  .venv/bin/pytest -q
-r requirements.txt
pytest>=8
httpx>=0.27
